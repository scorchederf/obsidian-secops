---
atomic_guid: "2db30061-589d-409b-b125-7b473944f9b3"
title: "Build Image On Host"
framework: "atomic"
generated: "true"
attack_technique_id: "T1612"
attack_technique_name: "Build Image on Host"
source_url: "https://github.com/redcanaryco/atomic-red-team/blob/master/atomics/T1612/T1612.yaml"
build_date: "2026-04-26 17:02:13"
executor: "sh"
aliases:
  - "2db30061-589d-409b-b125-7b473944f9b3"
  - "Build Image On Host"
platforms:
  - "containers"
tags:
  - "atomic"
  - "validation-test"
---

[[index|Home]] • [[kb/attack/index|ATT&CK]] • [[kb/tools/index|Tools]] • [[kb/defend/index|D3FEND]] • [[kb/car/index|CAR]] • [[kb/sigma/index|Sigma]] • [[kb/atomic/index|Atomic]] • [[kb/lolbas/index|LOLBAS]] • [[workspaces/index|Notes]]

# Build Image On Host

Adversaries may build a container image directly on a host to bypass defenses that monitor for the retrieval of malicious images from a public registry. An adversary may take advantage of that build API to build a custom image on the host that includes malware downloaded from their C2 server, and then they then may utilize Deploy Container using that custom image.

## Metadata

- Atomic GUID: 2db30061-589d-409b-b125-7b473944f9b3
- Technique: T1612: Build Image on Host
- Platforms: containers
- Executor: sh
- Dependency Executor: sh
- Source Path: atomics/T1612/T1612.yaml

## ATT&CK Mapping

- [[kb/attack/techniques/T1612-build_image_on_host|T1612]]

## Dependencies

Verify docker is installed.

### Prerequisite Check

```bash
which docker
```

### Get Prerequisite

```bash
if [ "" == "`which docker`" ]; then echo "Docker Not Found"; if [ -n "`which apt-get`" ]; then sudo apt-get -y install docker ; elif [ -n "`which yum`" ]; then sudo yum -y install docker ; fi ; else echo "Docker installed"; fi
```

Verify docker service is running.

### Prerequisite Check

```bash
sudo systemctl status docker  --no-pager
```

### Get Prerequisite

```bash
sudo systemctl start docker
```

## Executor

- name: sh

### Command

```bash
docker build -t t1612  $PathtoAtomicsFolder/T1612/src/
docker run --name t1612_container --rm -d -t t1612
docker exec t1612_container ./test.sh
```

### Cleanup

```bash
docker stop t1612_container
docker rmi -f t1612
```

## Source

- [Source YAML](https://github.com/redcanaryco/atomic-red-team/blob/master/atomics/T1612/T1612.yaml)
